#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n,i,m,j;
    printf("enter the number of string");
    scanf("%d",&n);
    char s[5];
    int shift[5];
    for(i=0;i<n;i++)
    {
        scanf("%s",s[i]);
    }

    printf("number of shifts");
    scanf("%d",&m);
    for(j=0;j<m;j++)
    {
        scanf("%d",&shift[i]);
    }

}
